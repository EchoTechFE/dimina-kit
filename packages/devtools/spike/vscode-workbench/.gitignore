node_modules/
dist/
baseline/
shots/
package-lock.json
coi-probe-result.json
# Real dimina project copied from the dimina/ submodule for the FileSystemProvider
# spike (I4). Not the submodule itself — a throwaway editable copy.
fixture-project/
# Runtime-seeded files land inside fixture-project (dimina.d.ts, jsconfig.json).
