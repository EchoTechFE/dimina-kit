import ReactDOM from 'react-dom/client'
import '../../design.css'
import HostSidebarDefault from '../../modules/host-sidebar-default/host-sidebar-default'

ReactDOM.createRoot(document.getElementById('root')!).render(<HostSidebarDefault />)
