{
  "extends": "@dimina-kit/typescript-config/base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src", "sync"],
  // The two worker entry points need the WebWorker (not DOM) lib — see
  // tsconfig.worker.json, which type-checks them as a separate program.
  "exclude": ["node_modules", "src/fs-core.worker.ts", "src/fs-query.worker.ts"]
}
